# Cross-Layer

```text
jdbc-dialect-xugu (aggregation)
  → connector / parser / binder / route / rewrite / sharding / federation / transaction / exception
proxy-dialect-xugu → proxy-backend-xugu + JDBC stack
```

Checklist: type key → services file → aggregation POM → support-matrix → tests.
